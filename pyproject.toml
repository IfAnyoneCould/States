[tool.poetry]
name = "States"
version = "0.1.0"
description = "States project for school"
authors = ["Jonah, Nes'et, Yerassyl, Nicholas, Anhad"]

[tool.poetry.dependencies]
python = ">=3.8.0,<3.9"
pygame = "^2.1.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"